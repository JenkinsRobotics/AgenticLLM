# assets/

Static media that ships with the framework — logos, banners, sample
demo files, reference images. Anything that isn't Python code, isn't
config, and isn't agent-writable state.

Convention borrowed from `python_hermes_agent/upstream/assets/`.
